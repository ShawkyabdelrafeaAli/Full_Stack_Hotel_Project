import { ReservationResponse } from './reservation-response';

describe('ReservationResponse', () => {
  it('should create an instance', () => {
    expect(new ReservationResponse()).toBeTruthy();
  });
});
